namespace NodeJS {
    export interface ProcessEnv {
        EMAIL_HOST: string;
        EMAIL_PORT: number;
        EMAIL_USERNAME: string;
        EMAIL_PASSWORD: string;
        EMAIL_FROM: string;
    }
}
